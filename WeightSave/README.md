## 学習済み重みファイルについて

`weights_vit.pth` は以下の理由により非公開としています。

- ファイルサイズが大きくGitHubの容量制限に抵触するため
- `training-vit.ipynb` を実行することで再現可能なため

### 重みファイルを用意する方法

`training-vit.ipynb` を実行すると  
`WeightSave/weights_vit.pth` が生成されます。
